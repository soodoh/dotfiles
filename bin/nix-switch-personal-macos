#!/usr/bin/env bash
set -euo pipefail
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$script_dir/lib/sudo-session.sh"
cd "$script_dir/.."
start_sudo_session
/usr/bin/sudo --set-home nix run .#darwin-rebuild -- switch --flake .#personal-macos
