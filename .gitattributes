# Vendored skill snapshots preserve upstream formatting verbatim.
dotfiles/profiles/common/.agents/skills/** -whitespace

# Colima renders its complete generated configuration through the linked file.
dotfiles/darwin/.colima/default/colima.yaml -whitespace
